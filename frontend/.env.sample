# Sample frontend env. bin/generate-env.sh writes the real .env.local from
# Terraform outputs. Copy this if you want to develop without infra.
VITE_API_BASE_URL=http://localhost:3001/api
VITE_COGNITO_AUTHORITY=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_REDIRECT_URI=http://localhost:3000/login/callback
VITE_COGNITO_DOMAIN=
